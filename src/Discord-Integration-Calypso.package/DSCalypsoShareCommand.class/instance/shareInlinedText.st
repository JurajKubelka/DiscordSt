private
shareInlinedText
	DSShareInlinedCodeSnippetCommand new
		method: method;
		sourceNode: sourceNode;
		selectedText: selectedText;
		selectedTextInterval: selectedTextInterval;
		morph: browserMorph;
		execute