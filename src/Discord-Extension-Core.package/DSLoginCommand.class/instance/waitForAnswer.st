protocol
waitForAnswer
	"Wait for a Semaphore signal. 
	Useful if you need to wait for a dialog answer.
	It is signaled after the window, does not matter if the user is authenticated or not."
	semaphore wait